<!-- 
    Diese Datei brauchst du, falls du einmal keine direkte Verbindung ins
    Internet hast, sondern stattdessen über einen HTTP/S-Proxy gehen musst. vgl.
    https://clojure.org/reference/deps_and_cli#_maven_proxies

    In so einem Fall, musst du die settings.xml "unter" deinem Home-Verzeichnis
    ${user.home}/.m2/settings.xml speichern.
-->
<settings>
  <proxies>
   <proxy>
      <active>true</active>
      <protocol>https</protocol>
      <host><!-- Hostname/IP deines Proxy-Hosts --> </host>
      <port>3128</port>
    </proxy>
  </proxies>
</settings>